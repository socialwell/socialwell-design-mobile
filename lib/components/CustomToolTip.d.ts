/// <reference types="react" />
interface Props {
    text: string;
    position: string;
    visible?: boolean;
}
declare function CustomToolTip({ text, position, visible }: Props): JSX.Element;
export default CustomToolTip;
